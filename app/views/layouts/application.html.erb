<body>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>

  <div style="margin-bottom: 20px; padding: 10px; background-color: #f0f0f0; border-radius: 5px;">
    <% if user_signed_in? %>
      <span>Olá, <strong><%= current_user.email %></strong>!</span>
      <span style="margin: 0 10px;">|</span>
      <%= link_to "Meus Artigos Salvos", articles_path %>
      <span style="margin: 0 10px;">|</span>
      <%= button_to "Sair", destroy_user_session_path, method: :delete, style: "display: inline-block;" %>
    <% else %>
      <%= link_to "Cadastrar-se", new_user_registration_path %>
      <span style="margin: 0 10px;">|</span>
      <%= link_to "Entrar", new_user_session_path %>
    <% end %>
  </div>
  <%# ========================================== %>

  <%= yield %> 
</body>
</html>